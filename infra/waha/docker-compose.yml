name: jasmine-waha

services:
  waha:
    image: devlikeapro/waha@sha256:ee94669b8b007f8108d2e93dc8ce1af78743919823bc6748facab35d8cc2c26c
    restart: always
    environment:
      WAHA_API_KEY: ${WAHA_API_KEY:?WAHA_API_KEY gerekli}
      WAHA_DASHBOARD_ENABLED: "false"
      WAHA_PRINT_QR: "false"
      WHATSAPP_DEFAULT_ENGINE: GOWS
      WHATSAPP_RESTART_ALL_SESSIONS: "true"
      WHATSAPP_SWAGGER_ENABLED: "false"
    expose:
      - "3000"
    volumes:
      - waha_sessions:/app/.sessions
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          node -e "fetch('http://127.0.0.1:3000/api/sessions',
          {headers:{'X-Api-Key':process.env.WAHA_API_KEY}})
          .then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
      interval: 15s
      timeout: 8s
      retries: 20
      start_period: 30s
    networks: [private, edge]

  session-watchdog:
    image: node:22-alpine@sha256:c610fcdfb1d5b4740dd70c284ed3cb16bb857e0f7166196e36a5501df7a3aa32
    restart: always
    depends_on:
      waha:
        condition: service_healthy
    environment:
      WAHA_API_KEY: ${WAHA_API_KEY:?WAHA_API_KEY gerekli}
      WAHA_INTERNAL_URL: http://waha:3000
      WAHA_WATCHDOG_INTERVAL_MS: "15000"
    command: ["node", "/app/session-watchdog.mjs"]
    volumes:
      - ./session-watchdog.mjs:/app/session-watchdog.mjs:ro
    networks: [private]

  caddy:
    image: caddy:2.8.4-alpine
    restart: always
    depends_on:
      waha:
        condition: service_healthy
    environment:
      WAHA_DOMAIN: ${WAHA_DOMAIN:?WAHA_DOMAIN gerekli}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks: [edge]

  outbox-worker:
    image: curlimages/curl:8.12.1
    restart: always
    environment:
      JASMINE_OUTBOX_URL: ${JASMINE_OUTBOX_URL:?JASMINE_OUTBOX_URL gerekli}
      CRON_SECRET: ${CRON_SECRET:?CRON_SECRET gerekli}
    command:
      - /bin/sh
      - -ec
      - |
        while true; do
          curl --fail --silent --show-error \
            --max-time 55 \
            -H "Authorization: Bearer $${CRON_SECRET}" \
            "$${JASMINE_OUTBOX_URL}" || true
          sleep 60
        done
    networks: [edge]

volumes:
  waha_sessions:
  caddy_data:
  caddy_config:

networks:
  private:
    internal: true
  edge:
